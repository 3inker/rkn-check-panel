# Автоопределение для всех текстовых файлов
* text=auto

# Принудительно конвертировать .bat файлы в CRLF при checkout (скачивании)
*.bat text eol=crlf

# При commit (отправке) конвертировать обратно в LF (для хранения в репо)